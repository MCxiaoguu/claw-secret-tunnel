node_modules/
dist/
coverage/
*.log
.DS_Store
.env
.env.*
# local OpenClaw checkout used only for testing/reference — never commit it here
openclaw/

# Local design/planning docs — kept on disk, never published
docs/
